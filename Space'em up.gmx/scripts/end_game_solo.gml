///void end_game_solo()

text = "Quand gameover solo/coop";
text = "Score";
string(scorePlayer);
text = "tactile"
string(tactile);

//Mise a jour du score
//Si le joueur est en tactile ou non
    if(tactile){
        //Si le joueur un fait un meilleur score qu'avant le sauvagarde
        if(scorePlayer > scoreTactile){
            scoreTactile = scorePlayer
        }
    }
    else{
        //Si le joueur un fait un meilleur score qu'avant le sauvagarde
        if(scorePlayer > scoreMax){
            scoreMax = scorePlayer
        }
    }

